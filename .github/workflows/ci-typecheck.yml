name: CI — Typecheck

on:
  workflow_call:
    inputs:
      node-version:
        description: Node.js version
        type: string
        default: "20"
      package-manager:
        description: Package manager (pnpm, npm, or bun)
        type: string
        default: pnpm
      working-directory:
        description: Working directory
        type: string
        default: "."
      typecheck-command:
        description: Typecheck command to run
        type: string
        default: typecheck
      nx-affected:
        description: Use Nx affected to only typecheck changed projects
        type: boolean
        default: false
      concurrency-group:
        description: Concurrency group name
        type: string
        default: ""

concurrency:
  group: ${{ inputs.concurrency-group || format('{0}-typecheck-{1}', github.workflow, github.ref) }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  typecheck:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: ${{ inputs.nx-affected && 0 || 1 }}

      - name: Setup Node.js
        uses: TQTuyen/ci-cd-toolkit/.github/actions/setup-node@main
        with:
          node-version: ${{ inputs.node-version }}
          package-manager: ${{ inputs.package-manager }}
          working-directory: ${{ inputs.working-directory }}

      - name: Setup Nx SHAs
        if: inputs.nx-affected
        uses: TQTuyen/ci-cd-toolkit/.github/actions/setup-nx@main

      - name: Typecheck
        if: "!inputs.nx-affected"
        shell: bash
        working-directory: ${{ inputs.working-directory }}
        run: |
          case "${{ inputs.package-manager }}" in
            pnpm) pnpm run ${{ inputs.typecheck-command }} ;;
            npm)  npm run ${{ inputs.typecheck-command }} ;;
            bun)  bun run ${{ inputs.typecheck-command }} ;;
          esac

      - name: Typecheck (Nx affected)
        if: inputs.nx-affected
        shell: bash
        working-directory: ${{ inputs.working-directory }}
        run: npx nx affected --target=${{ inputs.typecheck-command }}
